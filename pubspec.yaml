name: rayar_flutter_app
description: "Country Explorer - Dynamic Theming & Offline Sync"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.1.0
  cached_network_image: ^3.4.0
  shimmer: ^3.0.0
  go_router: ^14.8.0
  flutter_animate: ^4.5.0
  intl: ^0.20.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
